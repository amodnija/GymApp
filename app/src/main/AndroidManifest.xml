<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gymapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GymApp">
        <activity android:name=".ContactUs"></activity>
        <activity android:name=".Dashboard" />
        <activity android:name=".ForgotPassword" />
        <activity android:name=".RegisterStaffActivity" />
        <activity android:name=".DummyActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".routineInfo" />
        <activity android:name=".TrainingRoutines" />
        <activity android:name=".editMember" />
        <activity android:name=".memberList" />
        <activity android:name=".AddMemberActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>